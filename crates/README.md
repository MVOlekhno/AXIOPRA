# Rust Workspace — Planned

This directory will contain the deterministic AXIOPRA implementation after the verifier specification and fixtures are approved.

Planned responsibilities:

- Engineering IR and core domain types;
- Markdown/YAML artifact parser;
- JSON Schema validation;
- typed knowledge graph;
- rule engine;
- reports and graph generation;
- CLI;
- SDK and plugin interfaces.

No production crate should be added before its upstream specification, contracts, and test strategy are traceable.
